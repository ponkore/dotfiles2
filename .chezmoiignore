# fzf は Windows では展開しない（macOS / Linux は対応済み）
{{ if eq .chezmoi.os "windows" }}
dot_fzf.zsh.tmpl
dot_fzf.bash.tmpl
{{ end }}

# Windows では展開しないファイル
{{ if eq .chezmoi.os "windows" }}
dot_git-prompt.sh
dot_hgignore_global.tmpl
dot_config/htop
{{ end }}

# nyagos は Windows 専用シェルなので他 OS では展開しない
{{ if ne .chezmoi.os "windows" }}
dot_nyagos.tmpl
dot_config/nyagos/nyagos.lua
{{ end }}

# sql.cmd は SQLcl の Windows 用バッチラッパーなので他 OS では展開しない
{{ if ne .chezmoi.os "windows" }}
bin/sql.cmd
{{ end }}

# .claude.json は Claude Code が実行時に書き換える動的な状態ファイルのため管理対象外
.claude-config/jighead/.claude.json
